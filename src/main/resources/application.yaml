spring:
  application:
    name: lovable
  datasource:
    url: jdbc:postgresql://localhost:5432/lovable-test-db
    username: satabdomajumder
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        use_sql_comments: true
jwt:
  secret-key: your-super-long-secret-key-at-least-32-characters